{
  "name": "server-side",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server": "json-server --watch db.json --port 9000 --no-cors"
  },
  "keywords": [
    "start"
  ],
  "dependencies": {
    "express": "^4.17.1"
  }
}
